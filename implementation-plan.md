# 政治経済匿名広場 実装計画書

更新日: 2026-07-11
状態: 計画確定、実装未着手

## 0. この文書の役割

product-research.md は「何を、なぜ作るか」の正本である。

この文書は「どの順番で、どのファイルとデータを変更し、何をもって完成とするか」の実行計画である。

実装判断が衝突した場合:

1. 製品の目的と非交渉条件は product-research.md を優先する。
2. 作業順、移行、検証、完了条件は implementation-plan.md を優先する。
3. 古い調査文書は参考資料であり、上記二文書に反する場合は採用しない。

## 1. 目標

現在の「分類と根拠を先に要求する政治論点サイト」を、次の匿名ソーシャル掲示板へ段階的に置き換える。

**人の本音を覗く → 同じ側を見つける → 真逆の側へ返す → 返信を受ける → 言い合いと常連を見に戻る**

最初の製品版で成立させること:

- 登録せず読める。
- 本文だけで投稿できる。
- 投稿番号、直接返信、引用がある。
- スレッド内または板内の匿名IDがある。
- 勢い、新着、言い合い中を見分けられる。
- 議員、政党、政策ページからスレッドを読んで立てられる。
- 立場は任意で、同じ側、真逆、ごちゃ混ぜを切り替えられる。
- 未読と自分への返信が分かる。
- 強い言い合いを残しつつ、晒し、脅迫、差別、bot、コピペ爆撃を止められる。

## 2. 今回の対象外

最初の製品版が成立するまで実装しない。

- 全国会議員の完全同期
- AIによる長文要約
- 政治思想診断
- 人物評価点
- 全体カルマ
- フォロワー機能
- 個別DM
- 公式な討論勝敗
- 複雑な合意形成
- 高度な証拠スコア
- 自動ニュース転載
- 有料会員
- 広告

## 3. 現在の実装

### 3.1 実行環境

| 用途 | 現在の実装 |
| --- | --- |
| ローカル | server.js、Express、data 配下のJSON |
| 公開ビルド | worker/index.js、Cloudflare Worker、D1 |
| 画面 | public 配下のHTML、CSS、素のJavaScript |
| テスト | test/api.test.js、Node test runner |
| ビルド | Vite、Cloudflareプラグイン |

ローカル版とWorker版に同じロジックが重複している。新しいAPIは片方だけに実装せず、各作業単位で両方を更新する。

### 3.2 現在の主要ルート

画面:

- /
- /room/:roomId
- /new
- /politicians
- /politician/:politicianId
- /thread/:threadId
- /moderation

API:

- GET /api/board
- GET /api/home
- GET /api/rooms/:roomId
- GET /api/politicians
- GET /api/politicians/:politicianId
- GET /api/threads/:threadId
- POST /api/threads
- POST /api/threads/:threadId/positions
- POST /api/threads/:threadId/reactions
- POST /api/threads/:threadId/comments
- POST /api/threads/:threadId/comments/:commentId/helpfulness
- POST /api/reports
- 運営API

### 3.3 現在の投稿制約

スレ立てで現在必須:

- 部屋
- 8文字以上のタイトル
- 20文字以上の要約
- 30文字以上の本文
- タグ
- 出典URL
- 出典種別
- 対象
- 判断を促す問い
- 生活影響

コメントで現在必須:

- 本文
- 立場
- 主張種別
- 生活影響
- 事実投稿の場合は出典URL

この制約は新しい製品方針と逆である。ただし一度に削除せず、API互換を保ちながら任意項目へ移す。

### 3.4 現在不足しているもの

- 投稿番号
- 返信先
- 引用
- 匿名セッション
- 板内ID
- 参加者数
- 相互返信数
- 立場横断返信数
- 未読境界
- 自分への返信
- 次スレ
- スレッド種別
- 複数対象への紐付け
- 政党、政策、経済テーマの対象ページ
- コメントへの「わかる、草、いや違う、ソースは？」
- 会話中心の勢い計算

### 3.5 現在の順位が重視しているもの

現在は次が強く評価される。

- 出典
- 判断問い
- 生活影響
- 根拠付きコメント
- 見方が変わった反応

新しい順位では次へ変える。

- 異なる参加者
- 相互返信
- 返信を受けた投稿
- 直近の未読増加
- 立場横断返信

一人の連投、コピペ、同文反復は勢いへ入れない。

## 4. 実装原則

### 4.1 会話を先に作る

順番は固定する。

1. 本文だけで書ける。
2. 直接返せる。
3. 誰が返したか分かる。
4. 続きを見に戻れる。
5. 同じ側と真逆を探せる。
6. ニュースとデータを投下できる。

### 4.2 破壊的移行をしない

- 既存JSONを読み続けられる正規化処理を先に作る。
- D1は追加マイグレーションを使い、既存テーブルを削除しない。
- 古いフィールドは読み取り互換を残す。
- 新UIから使わなくなったフィールドも、移行完了までは保存内容を消さない。
- 本番データ削除を伴う作業は別承認とする。

### 4.3 Express版とWorker版を同時に完成させる

各マイルストーンの完了条件:

- server.jsで動く。
- worker/index.jsで同じ契約が動く。
- JSONとD1で同じレスポンス形を返す。
- APIテストが契約差を検出する。

### 4.4 小さな完成形を積み上げる

一つの大規模変更にしない。

- データ互換
- API
- 画面
- テスト
- ブラウザ検証

を一つの利用者行動ごとに完成させる。

### 4.5 偽の稼働感を作らない

- 本番でデモコメントを実利用者として表示しない。
- 同時接続、参加者数、勢いは実数だけを使う。
- テスト用データは開発環境と明示する。

## 5. 目標データモデル

既存のJSON payload方式を直ちに全面廃止しない。会話に必要なフィールドを追加し、検索や競合制御が必要な部分だけD1テーブルへ分離する。

### 5.1 スレッド

追加するフィールド:

| フィールド | 内容 |
| --- | --- |
| schemaVersion | 新旧正規化用。新規は2 |
| mode | mixed、same-side、opposition-welcome、debate、live、question、banter |
| targetRefs | 議員、政党、政策、経済テーマの複数対象 |
| lastCommentNo | 最後に割り当てた投稿番号 |
| participantCount | 異なる匿名参加者数 |
| mutualReplyCount | 相互返信の組数 |
| crossStanceReplyCount | 異なる立場間の返信数 |
| lastActivityAt | 最新投稿時刻 |
| continuationOf | 前スレID |
| nextThreadId | 次スレID |

既存の targetType、targetId、targetLabel は targetRefs の先頭へ正規化する。移行中は両方を返せるようにする。

### 5.2 コメント

追加するフィールド:

| フィールド | 内容 |
| --- | --- |
| schemaVersion | 新規は2 |
| number | スレッド内の連番 |
| actorHash | 匿名セッションをハッシュ化した内部識別子 |
| displayId | 公開する板内または日次ID |
| replyToId | 直接返信先コメントID |
| replyToNumber | 表示用の返信先番号 |
| quotedNumbers | 本文中で引用した投稿番号 |
| stance | support、oppose、unsure、other、またはnull |
| sourceUrl | 任意 |
| reactions | agree、funny、disagree、source-request |
| createdAt | 投稿日時 |

新規コメントでは claimType、impactAreas、helpfulness を必須にしない。旧コメントの表示互換のため読み取りは残す。

### 5.3 匿名セッション

方針:

- 初回アクセスでランダムな匿名セッションCookieを発行する。
- CookieはHttpOnly、SameSite=Laxとし、本番HTTPSではSecureを付ける。
- サーバーとD1へCookie生値を保存しない。
- SHA-256でactorHashへ変換する。
- displayIdは actorHash、板ID、日付から生成する。
- Cookieを削除すれば別人になれるため、本人確認や不正防止の唯一手段にはしない。
- レート制限は現在の接続元フィンガープリントと匿名セッションを併用する。

初期は日次または板単位IDとし、長期仮名・トリップはM7へ送る。

### 5.4 投稿番号

ローカルJSON:

- スレッドごとに nextCommentNo を保持する。
- 同一スレッドへの書き込みを直列化する。
- 既存コメントは古い順に1から番号を割り当てる。

D1:

- 新規マイグレーションで thread_counters を追加する。
- thread_id を主キーにする。
- 原子的な更新で次番号を取得する。
- comments.payloadにも確定番号を保存する。
- 同一スレッドで番号が重複しないテストを追加する。

### 5.5 コメント反応

D1に comment_reactions を追加する。

想定列:

- comment_id
- actor_hash
- reaction
- created_at

同じ匿名参加者が同じコメントへ同じ反応を重複できない一意制約を付ける。

ローカルJSONでは、公開集計と匿名参加者ごとの反応履歴を分離する。

### 5.6 話題別立場

M5で actor_stances を追加する。

キー:

- actor_hash
- target_type
- target_id

値:

- stance
- updated_at

公開するのは集計と、投稿者がその投稿で表示を選んだ立場だけである。個人の全立場一覧を公開しない。

## 6. マイルストーン

### M0. 互換レイヤーと契約テスト

目的:

新旧データを同じ画面で読める土台を作り、その後の変更で既存データを壊さない。

主な変更:

- スレッドとコメントのschemaVersion導入
- 新旧スレッド・コメントの互換正規化
- 既存targetをtargetRefsへ変換
- 既存コメントへ表示時の投稿番号を付与
- APIレスポンスの契約テスト
- Express版とWorker版のレスポンス項目比較
- D1追加マイグレーションの準備

対象ファイル:

- server.js
- worker/index.js
- drizzle/0001_conversation_core.sql
- test/api.test.js
- 必要に応じて新しいテスト補助ファイル

完了条件:

- 現在のdata配下を変更せず読み込める。
- 旧コメントにも安定した投稿番号が表示用に付く。
- 新フィールドがなくてもAPIが落ちない。
- 既存4テストが通る。
- 新旧データ互換テストが通る。

ロールバック:

- 新マイグレーションを適用しなくても旧コードが動く状態を保つ。
- この段階では画面を切り替えない。

規模: M

### M1. 本文だけの投稿、投稿番号、返信、引用

目的:

最重要の会話循環を成立させる。

API変更:

- コメント必須項目を本文だけに近づける。
- stance、claimType、impactAreas、sourceUrlを任意にする。
- replyToIdを受け取る。
- 返信先が同じスレッド内に存在することを検証する。
- comment numberを割り当てる。
- quotedNumbersを抽出または明示保存する。
- コメントを古い順の平たい時系列で返す。

画面変更:

- コメントをカード型の採点画面から、番号付きの平たい時系列へ変更する。
- 各投稿に返信ボタンを置く。
- 返信時は投稿欄へ >>番号 を挿入する。
- 引用元へ移動できる。
- 引用先から戻れる。
- 投稿フォームは本文を最初に置く。
- 立場、URL、名前は任意の詳細へ下げる。
- 主張種別と生活影響を通常フォームから外す。
- 事実投稿への出典必須を外す。

対象ファイル:

- server.js
- worker/index.js
- public/thread.html
- public/thread.js
- public/shared.js
- public/styles.css
- test/api.test.js
- drizzle/0001_conversation_core.sql

必須テスト:

- 本文だけでコメントできる。
- 投稿番号が1ずつ増える。
- 同時投稿でも番号が重複しない。
- 別スレッドのコメントへ返信できない。
- 存在しない返信先を拒否する。
- 返信先と引用番号がレスポンスへ残る。
- stanceとsourceUrlを付けても付けなくても投稿できる。
- lockedとslow modeが引き続き効く。

ブラウザ受入:

- 初見で投稿欄を見つけられる。
- 二操作以内で特定投稿へ返信できる。
- 390px幅で投稿番号、本文、返信ボタンが重ならない。
- 長い引用でも本文が読める。
- キーボードだけで返信と投稿ができる。

規模: L

### M2. 匿名ID、未読、ウォッチ、返信通知

目的:

同じ人間を識別し、会話の続きを見に戻れるようにする。

主な変更:

- 匿名セッションCookie
- actorHashとdisplayId
- コメントへのdisplayId表示
- 板または日次単位でIDを安定させる
- スレッドごとのlastSeenCommentNo
- 未読境界
- ウォッチ中スレッド
- 自分の投稿への返信一覧
- ヘッダーの未読件数

保存方針:

- lastSeenとウォッチは初期はブラウザ保存を使用する。
- 自分への返信は匿名セッションとactorHashで取得する。
- 複数端末同期は行わない。

追加API候補:

- GET /api/activity
- POST /api/threads/:threadId/watch
- DELETE /api/threads/:threadId/watch

ブラウザ保存だけで十分な機能は、不要にD1へ書かない。

対象ファイル:

- server.js
- worker/index.js
- public/shared.js
- public/thread.js
- public/home.js
- public/index.html
- public/styles.css
- test/api.test.js

必須テスト:

- 同じ匿名セッションは同じ板・同じ日で同じdisplayIdになる。
- 別セッションは異なるdisplayIdになる。
- Cookie生値をAPIへ返さない。
- 自分への直接返信だけactivityへ出る。
- lastSeenより新しい番号だけ未読になる。
- Cookieを持たない閲覧者にも正常に発行される。

ブラウザ受入:

- 既読と未読の境界が明確である。
- 自分への返信へ一操作で移動できる。
- ウォッチ解除が分かる。
- 数字やラベルがレイアウトを動かさない。

規模: L

### M3. 会話中心のホームと三板

目的:

最初の3秒で「人が今話している」と感じさせる。

板:

- politics: 政治総合
- economy: 生活と経済
- live: 実況・速報

旧部屋の移行:

| 旧部屋 | 新しい板 | 旧URLの扱い |
| --- | --- | --- |
| money | politics | politicsへ転送し、政治とカネタグを付ける |
| security | politics | politicsへ転送し、安全保障タグを付ける |
| election | politics | politicsへ転送し、選挙タグを付ける |
| tax | economy | economyへ転送し、税・社会保険タグを付ける |
| prices | economy | economyへ転送し、物価タグを付ける |
| boj | economy | economyへ転送し、日銀・相場タグを付ける |

実況は新設する。既存URLを404にしない。

ホーム主タブ:

- 勢い
- 新着
- 言い合い中
- ウォッチ中

ホームから下げる:

- 国会画像のヒーロー
- 生活影響フィルター
- 根拠が集まった議論
- 証拠率
- 見方が変わった順位
- 教育的説明

スレッド行へ出す:

- タイトル
- 板と対象
- 直近の発言
- 返信数
- 異なる参加者数
- 相互返信
- 勢い
- 未読

勢い計算:

- 直近の異なるactorHash
- 相互返信
- 返信を受けた投稿
- 経過時間
- 一人当たり上限

言い合い中:

- 異なる複数人が相互返信している。
- stanceがある場合は立場横断返信を加点する。
- 悪口、通報、怒り語そのものは加点しない。

対象ファイル:

- server.js
- worker/index.js
- public/index.html
- public/home.js
- public/room.html
- public/room.js
- public/shared.js
- public/styles.css
- data/threads.jsonの開発用マッピング
- test/api.test.js

必須テスト:

- 一人の連投だけでは勢いが上がり続けない。
- 二人以上の相互返信で言い合い中へ入る。
- 旧部屋URLが適切な新板へ到達する。
- home payloadにhot、new、arguing、watchedがある。
- 同時接続や参加者の偽数値を返さない。

ブラウザ受入:

- 390px、768px、1440pxで主タブが収まる。
- 最初の画面に最新の人間の発言が見える。
- 巨大な説明やヒーローが会話を押し下げない。
- 空の板は空と表示される。

規模: L

### M4. 議員、政党、政策、経済テーマの掲示板化

目的:

「誰か・何かについて語る」ための恒久的な入口を作る。

対象種類:

- politician
- party
- policy
- economy

データ:

- data/politicians.jsonは継続利用する。
- data/entities.jsonを新設し、政党、政策、経済テーマを持つ。
- スレッドはtargetRefsで複数対象へ紐付ける。

画面:

- 議員ページ最上部に勢いのあるスレッドを出す。
- 支持、批判、ごちゃ混ぜの切替を置く。
- この議員でスレを立てる。
- 政党、政策、経済テーマも同じ共通テンプレートを使う。
- 公式プロフィールや資料は下段または折りたたみにする。
- 感情集計グラフを主役から下げる。

ルート候補:

- /politician/:id は維持
- /party/:id
- /policy/:id
- /economy/:id

API候補:

- GET /api/entities
- GET /api/entities/:type/:id
- GET /api/entities/:type/:id/threads

スレ立て:

- 対象ページからタイトルと本文だけで開始できる。
- 対象は自動入力する。
- URL、要約、判断問い、影響分類は任意へ下げる。
- 重複候補は警告するが投稿を強制停止しない。

対象ファイル:

- server.js
- worker/index.js
- data/entities.json
- public/politician.html
- public/politician.js
- 共通対象ページ用HTML・JS
- public/new.html
- public/new-thread.js
- public/shared.js
- public/styles.css
- test/api.test.js

必須テスト:

- 議員ページから作成したスレッドが同じ議員ページへ出る。
- 複数対象のスレッドが全対象ページへ出る。
- 存在しない対象を拒否する。
- 旧targetフィールドだけのスレッドも対象ページへ出る。
- sourceUrlなしでスレッドを作れる。
- タイトルと本文の最低条件だけで作れる。

ブラウザ受入:

- 議員ページを開くとプロフィールより会話が先に見える。
- 支持、批判、ごちゃ混ぜが分かる。
- 一操作でスレ立て画面へ行ける。
- 対象名が長くてもボタンやタブが崩れない。

規模: L

### M5. 任意立場、同じ側、真逆、反対歓迎

目的:

話題ごとの仲間と反対者を探せるようにする。

立場:

- 議員: support、oppose、unsure
- 政党: support、oppose、unsure
- 政策: support、oppose、unsure、other
- 投稿単位: 任意

スレッドmode:

- mixed
- same-side
- opposition-welcome
- debate
- live
- question
- banter

表示:

- ごちゃ混ぜ
- 自分と同じ
- 自分と反対
- 立場未表明
- 相互返信だけ

同じ側スレ:

- 公開のままにする。
- 投稿時にスレッド内立場を選ぶ。
- 思想の真偽を運営が審査しない。
- 反対者は閲覧でき、対抗スレを立てられる。

反対歓迎:

- 反対側の新着欄へ候補として出す。
- 個別DMは使わない。
- 同じ相手への連続呼び出しを制限する。
- 拒否とミュートを提供する。

ホーム:

- 主タブとは別に、ごちゃ混ぜ、同じ側、真逆のフィルターを置く。
- 立場未登録なら同じ側・真逆を無理に表示しない。

対象ファイル:

- server.js
- worker/index.js
- D1のactor_stancesテーブル
- ローカルの立場保存
- public/home.js
- public/thread.js
- public/new-thread.js
- 対象ページ
- public/styles.css
- test/api.test.js

必須テスト:

- 立場なしでも閲覧、投稿できる。
- 話題ごとに異なる立場を持てる。
- 同じ側と真逆が正しく分類される。
- 立場変更後の表示が更新される。
- 個人の全立場履歴を公開APIが返さない。
- ミュートした相手からの呼び出しを出さない。
- 反対歓迎が一人への大量通知に使えない。

ブラウザ受入:

- 立場選択が投稿前の障害にならない。
- ごちゃ混ぜが常に既定である。
- 同じ側と真逆の切替が明確である。
- フィルター変更でスクロール位置が不自然に飛ばない。

規模: L

### M6. コメント反応と「ソースは？」燃料導線

目的:

社会的報酬を返し、必要になった時だけ資料を会話へ入れる。

反応:

- わかる
- 草
- いや違う
- ソースは？

ルール:

- 反応は真実判定に使わない。
- 反応数で投稿を自動非表示にしない。
- 同一匿名参加者の重複反応を止める。
- ソースは？からURLまたは燃料カードを返信できる。

燃料カード:

- ニュースURL
- 議員発言
- 国会会議録
- 投票結果
- 法案
- 統計
- 動画
- SNS投稿
- 過去スレ

初期はURLのタイトル、ホスト、時刻、対象だけを表示する。記事本文は保存しない。

対象ファイル:

- server.js
- worker/index.js
- D1 comment_reactions
- public/thread.js
- public/shared.js
- public/styles.css
- test/api.test.js

必須テスト:

- 四反応を個別に保存できる。
- 同じ参加者の同一反応を重複できない。
- 別反応は可能である。
- URLが安全なHTTP/HTTPSだけに制限される。
- 反応数が勢いへ無制限加点されない。

規模: M

### M7. 次スレ、継続仮名、板文化

目的:

一回の議論を常連文化へつなげる。

機能:

- 投稿上限への接近表示
- 次スレ作成
- continuationOfとnextThreadId
- 前スレへの導線
- 板内継続仮名
- 任意トリップ
- 板ローカルルール
- 定例実況

トリップ:

- パスワード自体を保存しない。
- 公開トリップだけを表示する。
- 本人確認や信頼認証とは呼ばない。

完了条件:

- 上限到達前に次スレを作れる。
- 新旧スレを往復できる。
- 継続仮名なしでも参加できる。
- ローカルルールは短く、投稿前の長文同意にしない。

規模: M

## 7. モデレーション作業線

モデレーションは最後に足さず、各マイルストーンへ同時実装する。

### Phase A: 会話コアと同時

- 返信先通報
- ミュート
- レート制限
- slow mode
- ロック
- 同文反復検知

### Phase B: 立場機能と同時

- 連続呼び出し制限
- 反対側への大量通知防止
- 一人による複数板投下の検知
- 立場機能を使った標的化の通報理由

### Phase C: 公開前

- 脅迫
- 個人情報
- 属性攻撃
- 継続嫌がらせ
- なりすまし
- bot
- 組織投稿
- 違法転載

の判断手順を運営画面へ反映する。

現在の通報理由「誹謗中傷、個人攻撃、デマ、スパム、荒らし、その他」は、具体的な危害単位へ見直す。

## 8. テスト戦略

### 8.1 API

各変更で最低限確認する。

- 正常系
- 必須・任意境界
- 存在しないID
- 別スレッド参照
- 重複投稿
- レート制限
- locked
- slow mode
- 旧データ読み取り
- JSON保存後の再起動
- D1保存後の再読込

### 8.2 競合

特に投稿番号で確認する。

- 同一スレッドへ10件以上を同時投稿する。
- 番号が一意かつ連続する。
- コメント欠落がない。
- lastCommentNoと実データが一致する。

### 8.3 ブラウザ

必須画面:

- ホーム
- 板
- スレッド
- スレ立て
- 議員
- 政党
- 政策
- 運営

必須幅:

- 390 x 844
- 768 x 1024
- 1440 x 900

確認:

- 文字とボタンが重ならない。
- 長い日本語がはみ出さない。
- 投稿欄が操作中に飛ばない。
- 引用先へ移動して戻れる。
- 未読境界が見える。
- タブと立場フィルターがモバイルで収まる。
- キーボード操作とフォーカスが分かる。
- 空状態、エラー、slow mode、locked状態が分かる。

### 8.4 Worker/D1

- Vite buildが通る。
- Worker側APIがExpress版と同じ項目を返す。
- マイグレーションを空DBへ適用できる。
- 既存DBへ追加適用できる。
- 運営APIが一般公開されない。

### 8.5 計測

利用行動の外部送信や新しいテレメトリーは、ユーザーの明示承認なしに追加しない。

最初は次で検証する。

- API集計
- 開発環境の操作試験
- 招待利用者への観察と聞き取り
- 個人を特定しない手動集計

## 9. 移行とロールバック

### 9.1 JSON

実装前:

- data/threads.json
- data/comments.json
- data/reports.json

のバックアップを作る。

移行:

- 読込時に旧形式を正規化する。
- 保存はschemaVersion 2で行う。
- 初回保存前にバックアップを残す。
- 旧フィールドを直ちに消さない。

### 9.2 D1

- 0001_conversation_core.sqlは追加のみとする。
- 適用前にD1 exportを取得する。
- 新列・新テーブルへ書き始めても旧payloadを残す。
- ロールバックは旧コードへ戻せる期間を確保する。
- 破壊的なテーブル再作成は別作業にする。

### 9.3 画面

必要なら環境変数または一時的な機能フラグで新旧画面を切り替える。

ただし、長期に二画面を保守しない。受入基準を満たしたら旧画面を削除する。

## 10. コミット単位

推奨する論理コミット:

1. データ互換とマイグレーション
2. コメント番号と返信API
3. 平たい時系列と返信UI
4. 匿名IDと未読
5. 会話中心ホームと三板
6. 対象ページ共通化
7. 任意立場と同じ側・真逆
8. コメント反応と燃料カード
9. 次スレと継続仮名
10. 公開前モデレーション

各コミット前に、対象APIテストとブラウザの狭い確認を行う。複数マイルストーンを一つの巨大コミットへ混ぜない。

## 11. 依存関係

必須順:

M0 → M1 → M2 → M3 → M4 → M5

M6はM1後に着手できるが、ホームや立場機能より優先しない。

M7はM3とM4の利用体験が安定してから行う。

モデレーション、テスト、移行は全マイルストーンに並行する。

## 12. 最初の実装着手単位

次に開始する作業はM0とM1前半に限定する。

具体的な成果:

1. 旧データ互換の正規化
2. 投稿番号
3. replyToId
4. 本文だけのコメント投稿
5. 平たい時系列
6. 返信ボタンと >>番号
7. APIテスト
8. モバイル確認

この作業では、ホーム全面改修、三板移行、立場マッチ、データ自動取得へ手を広げない。

## 13. 全体完了条件

最初の製品版は、次がすべて成立した時に完成とする。

- 初見で人間の会話が主役に見える。
- 本文だけで投稿できる。
- 投稿番号、引用、直接返信が機能する。
- 匿名IDで同じ人をスレッドまたは板の中で見分けられる。
- 未読と自分への返信が分かる。
- 勢い、新着、言い合い中を比較できる。
- 一人の連投だけで勢いを作れない。
- 議員、政党、政策、経済テーマのページが掲示板として動く。
- 同じ側、真逆、ごちゃ混ぜを任意で切り替えられる。
- 反対歓迎が嫌がらせ通知装置になっていない。
- ニュースと一次情報は任意の燃料として投下できる。
- 強い批判、皮肉、煽り、冗談、脱線が残る。
- 脅迫、晒し、差別、bot、コピペ爆撃を運営が止められる。
- Express/JSONとWorker/D1の機能差がない。
- npm run check、npm test、npm run buildが成功する。
- 390px、768px、1440pxで主要画面が崩れない。
- 旧データを失わず移行できる。

## 14. 現在の進捗

| 項目 | 状態 |
| --- | --- |
| 製品定義 | 完了 |
| 実装計画 | 完了 |
| M0 互換レイヤー | 未着手 |
| M1 返信コア | 未着手 |
| M2 匿名ID・未読 | 未着手 |
| M3 ホーム・三板 | 未着手 |
| M4 対象ページ | 未着手 |
| M5 同じ側・真逆 | 未着手 |
| M6 反応・燃料 | 未着手 |
| M7 次スレ・板文化 | 未着手 |

計画書があることと、製品が実装済みであることを混同しない。現在の画面は旧プロトタイプのままである。
